/* Ternary Operator (? :):
A shorthand for a simple if...else statement. 
It's an operator, not a statement, and it returns a value.*/

let age = 16;
let adultStatus = (age >= 18) ? "Adult" : "Minor";
console.log(adultStatus);

// my example

let number = 10;
let numberStatus = (number === 1) ? "1" : "Not 1";
console.log(numberStatus);